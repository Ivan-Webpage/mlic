import { NgModule } from '@angular/core';
import { SharedModule } from '../../shared.module';
@NgModule({
  declarations: [],
  imports: [
    SharedModule, // 共用套件
  ]
})
export class ArticleModule { }
